# 🎸 吉他社點歌系統 - 開發進度總覽

**最後更新時間**：2026-01-15 12:37 (UTC+8)  
**當前版本**：**v3.3.0**  
**GitHub Repo**：https://github.com/cagoooo/song  
**線上版本**：https://cagoooo.github.io/song/  
**Firebase 專案 ID**：`guitar-ff931`

---

## 🚀 快速開始

```bash
# 啟動開發伺服器
cd h:\song
npm run dev
# 訪問 http://localhost:5173/song/

# TypeScript 類型檢查
npm run check

# 部署到 GitHub
git add .
git commit -m "描述"
git push origin main
```

---

## ✅ v3.3.0 已完成功能 (2026-01-15)

### 1. 手機端 UI 全面優化

#### SongCard（可選歌單）
- ✅ 水平單行佈局（不再垂直排列）
- ✅ 歌曲標題 `text-lg` (18px)，手機端更大
- ✅ 點播按鈕 `h-11` (44px)，更好按
- ✅ 手機端隱藏音符圖標節省空間
- ✅ 管理員按鈕改為純 icon 按鈕
- ✅ 移除 framer-motion，改用純 CSS 動畫

#### RankingBoard（人氣排行榜）
- ✅ flex-wrap 換行佈局，確保按鈕不被切掉
- ✅ 票數字體 `text-lg` (18px)
- ✅ 操作按鈕 `w-10 h-10` (40px)，更好按
- ✅ 吉他譜/歌詞按鈕有色背景更明顯
- ✅ Tooltip z-index 設為 100 確保顯示

#### SuggestionCard（社群推薦）
- ✅ 減少留白 `p-3` 取代 `p-4`
- ✅ 歌曲標題 `text-lg` (18px)
- ✅ 快速連結按鈕 `h-9` (36px)，更好按
- ✅ 按鈕有色背景（indigo/rose）更明顯
- ✅ 移除「快速連結：」文字標籤節省空間
- ✅ 推薦理由字體 `text-sm` (14px)

#### ShareButton（分享對話框）
- ✅ QR Code 縮小至 160px
- ✅ 減少留白 `p-4` 取代預設
- ✅ 移除 framer-motion 動畫
- ✅ 手機端隱藏「掃描 QR Code」標籤

### 2. 動畫效能優化

| 組件 | 原本 | 現在 |
|------|------|------|
| VoteOverlay | framer-motion AnimatePresence | 純 CSS @keyframes |
| SongCard | motion.div 入場動畫 | animate-in fade-in slide-in |
| ShareButton | motion.div spring | animate-in zoom-in-95 |
| 社群按鈕 hover | motion.div whileHover | CSS hover:scale-105 |

### 3. 搜尋關鍵字優化

- ✅ 新增 `EXCLUDED_ARTISTS` 過濾器
- ✅ 排除選項：不確定、多人翻唱、經典老歌、未知歌手
- ✅ 吉他譜/歌詞搜尋不再包含無效關鍵字

### 4. 管理員預設排行榜

- ✅ `MobileTabView.tsx` 新增 `isAdmin` prop
- ✅ 管理員登入後自動切換到「排行榜」Tab
- ✅ 使用 `useEffect` + `useRef` 監聽登入狀態

---

## 📁 專案結構

```
h:\song\
├── package.json                    # v3.3.0
├── SESSION_PROGRESS.md             # 進度記錄
├── FUTURE_DEVELOPMENT_GUIDE.md     # 未來開發指南
├── firestore.rules                 # Firebase 安全規則
│
├── client/src/
│   ├── components/
│   │   ├── MobileTabView.tsx       # 手機版 Tab（含 isAdmin）
│   │   ├── ShareButton.tsx         # 分享對話框（純 CSS）
│   │   ├── SearchBar.tsx           # 搜尋欄
│   │   ├── ScrollToTop.tsx         # 返回頂部按鈕
│   │   │
│   │   ├── RankingBoard/
│   │   │   ├── RankingBoard.tsx    # 排行榜主組件（含彈奏標記）
│   │   │   ├── RankingHeader.tsx   # 標題區
│   │   │   ├── RankingBadge.tsx    # 排名徽章
│   │   │   └── useRankingData.ts   # 資料 Hook（含搜尋 URL）
│   │   │
│   │   ├── SongList/
│   │   │   ├── SongList.tsx        # 歌曲列表
│   │   │   ├── SongCard.tsx        # 歌曲卡片（純 CSS）
│   │   │   ├── VoteOverlay.tsx     # 點播成功動畫（純 CSS）
│   │   │   ├── VirtualSongList.tsx # 虛擬滾動
│   │   │   └── useVoting.tsx       # 投票 Hook
│   │   │
│   │   └── SongSuggestion/
│   │       ├── SongSuggestion.tsx  # 主組件
│   │       ├── SuggestionCard.tsx  # 推薦卡片（含過濾搜尋）
│   │       └── SuggestionForm.tsx  # 表單對話框
│   │
│   ├── hooks/
│   │   ├── use-suggestions.ts      # 推薦功能 Hook
│   │   ├── use-user.ts             # 使用者 Hook
│   │   ├── use-toast.ts            # Toast Hook
│   │   └── useReduceMotion.ts      # 減少動畫 Hook
│   │
│   ├── lib/
│   │   ├── firebase.ts             # Firebase 設定
│   │   ├── firestore.ts            # Firestore API
│   │   └── auth.ts                 # 認證相關
│   │
│   └── pages/
│       └── Home.tsx                # 主頁面
```

---

## 🎯 RWD 設計規範

### 手機端 (< 640px / sm)

| 元素 | 樣式 |
|------|------|
| 歌曲標題 | `text-lg` (18px) + `font-bold` |
| 歌手名稱 | `text-sm` (14px) |
| 票數 | `text-lg` / `text-xl` (18-20px) |
| 操作按鈕 | `w-10 h-10` / `h-11` (40-44px) |
| 按鈕圖標 | `w-5 h-5` (20px) |
| 卡片內距 | `p-2.5` / `p-3` |
| 區塊間距 | `gap-2` / `mb-2` |

### 桌面端 (≥ 640px / sm:)

| 元素 | 樣式 |
|------|------|
| 歌曲標題 | `text-base` (16px) |
| 票數 | `text-lg` (18px) |
| 操作按鈕 | `w-9 h-9` / `h-10` (36-40px) |
| 按鈕圖標 | `w-4 h-4` (16px) |
| 卡片內距 | `p-3` / `p-4` |

---

## ⚡ 效能優化原則

1. **優先使用 CSS 動畫**
   - `animate-in fade-in slide-in-from-bottom-2`
   - `transition-all duration-150`
   - `hover:scale-105 active:scale-95`

2. **避免 framer-motion**
   - 手機端效能較差
   - 使用純 CSS 替代

3. **GPU 加速屬性**
   - `transform` (scale, translate)
   - `opacity`
   - `will-change-transform`

4. **動畫時長**
   - 入場動畫：150-200ms
   - Hover 效果：100-150ms
   - 淡出動畫：100-150ms

---

## 🔧 待優化/可繼續的方向

1. **更多效能優化**
   - 檢查 `RankingBoard` 的 framer-motion 使用
   - 考慮移除排名變化動畫

2. **功能增強**
   - 歌曲搜尋功能優化
   - 更多分享選項

3. **測試**
   - 在不同手機型號測試
   - 確認所有動畫流暢

---

## 📝 Git 歷史

```
8d3704e - v3.3.0: 手機端 UI 全面優化 - 加大字體按鈕、減少留白、效能提升
2b1f364 - v3.2.0: UI/UX 優化 - 社群推薦效能提升、管理員預設排行榜、快速連結修正
```

---

## ⚠️ 注意事項

- 開發伺服器預設在 `http://localhost:5173/song/`
- 使用 `npm run check` 檢查 TypeScript 類型
- 部署前確認所有編譯通過
- Firebase 規則已部署，包含 `playedSongs` collection
